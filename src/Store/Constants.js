const STATES = {
  myInfo: 'myInfo',
  categories: 'Categories',
  postList: 'postList',
  formStyles: 'formStyles'
};

const GETTERS = {
  splitCategoryName: 'splitCategoryName'
};

const MUTATIONS = {

};

const ACTIONS = {

};

export default {
  ...STATES,
  ...GETTERS,
  ...MUTATIONS,
  ...ACTIONS
};
