const STATES = {
  myInfo: 'myInfo',
  categories: 'Categories',
  postList: 'postList'
};

const GETTERS = {
  splitCategoryName: 'splitCategoryName',
  filterPostList: 'filterPostList'
};

const MUTATIONS = {

};

const ACTIONS = {

};

export default {
  ...STATES,
  ...GETTERS,
  ...MUTATIONS,
  ...ACTIONS
};
